- 1️⃣ What is the difference between var, let, and const?

const is a unchangeable value assignment

let is easily changable assigned and declared 

var was popular is past but for declaration issue, it is discontinued and we shouldnt use that

    - 2️⃣ What is the spread operator (...)?

    spread operator devides the value in each part and make a another array or object

    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    map() transforms the array with processed or changed values
    filter() returns true when condition passes and ignores when not true
    forEach runs the loop through the every item of the array

    - 4️⃣ What is an arrow function?
    Arrow function is the shortcut writing of traditional functions for smaller code text

    - 5️⃣ What are template literals?
    template literals are the way of using backticks ` to make code easier to understand

